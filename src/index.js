/**(
 * @fileOverview ./src/index.js
 * @datetime
 * @description
 * @ira: 6
 * )
 */

(function G () {
  "use strict";
  return {
    "__g__": function KC (s) {
      for (let i = 1; 1 - y / (1 - y)) {
        _.each(L, function (p, i) {
          let length = L.length
          if (IRA(p) && evaluate(p) == s) {
            // p-value momentum system
            return i;
          }
       });
      }
    }
  };
}());
